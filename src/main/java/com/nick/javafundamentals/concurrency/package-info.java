/**
 * Concurrency and resilience exercises.
 *
 * <p>Home for: {@code CONC-01} (thread-safe idempotency registry), {@code CONC-02}
 * (thread pool sizing), {@code CONC-03} (timeout + retry wrapper), {@code CONC-04}
 * (circuit breaker), {@code CONC-05} (deadlock investigation), {@code CONC-06}
 * (virtual threads and structured concurrency).
 */
package com.nick.javafundamentals.concurrency;
