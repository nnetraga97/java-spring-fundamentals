/**
 * Messaging and distributed-systems exercises.
 *
 * <p>Home for: {@code DIST-01} (transactional outbox), {@code DIST-02} (idempotent
 * consumer), {@code DIST-03} (partitioning / ordering), {@code DIST-04} (retry +
 * dead letter), {@code DIST-05} (saga), {@code DIST-06} (distributed rate limiting).
 *
 * <p>These can be modeled in-process (queues, executors, in-memory stores) before
 * reaching for a real broker — the point is the pattern and its failure modes.
 */
package com.nick.javafundamentals.messaging;
