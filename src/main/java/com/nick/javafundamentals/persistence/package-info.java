/**
 * Persistence, transactions, and SQL exercises.
 *
 * <p>Home for: {@code DATA-01} (entity lifecycle / dirty checking), {@code DATA-02}
 * (transaction boundaries / rollback), {@code DATA-03} (N+1 detection),
 * {@code DATA-04} (optimistic locking), {@code DATA-05} (index design),
 * {@code DATA-06} (cursor pagination), {@code DATA-07} (batch writes + pool),
 * and {@code SPR-04} (transaction proxy self-invocation trap).
 */
package com.nick.javafundamentals.persistence;
