/**
 * Domain modeling exercises: records, enums, sealed types, state machines.
 *
 * <p>Home for: {@code JAVA-02} (transaction authorization state machine),
 * {@code JAVA-03} (generics / validator pipeline), {@code JAVA-04} (exception
 * translation). Keep this package free of Spring imports so {@code TEST-01} stays
 * true: domain logic should be testable without loading the framework.
 */
package com.nick.javafundamentals.domain;
