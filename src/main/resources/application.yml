spring:
  application:
    name: java-spring-fundamentals
  profiles:
    active: local

# Actuator defaults are intentionally conservative. PROD-01 asks you to expose
# health/liveness/readiness deliberately and restrict the rest.
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
